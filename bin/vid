#!/usr/bin/env bash

if [[ "$1" = "-h" ]]; then
    shift
    mpv --ao=alsa:device=[plughw:0,3] --x11-name youtube-vid -loop=inf $(youtube-dl --no-playlist -g "$*")
else
    mpv --x11-name youtube-vid -loop=inf $(youtube-dl --no-playlist -g "$*")
fi
